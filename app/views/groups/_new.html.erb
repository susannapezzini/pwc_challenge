
    <%= simple_form_for [@subproduct, @group] do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :name %>
    </div>

    <div class="form-actions mb-5">
      <%= f.button :submit %>
    </div>
  <% end %>
